import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    label: {
        display: 'flex',
        alignItems: 'center',
        cursor: 'pointer',
    },
}));
