import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
    strategyList: {
        marginBottom: '0.5rem',
    },
    strategyListChip: {
        margin: '0.25rem',
    },
    strategyListHeader: {
        textAlign: 'center',
        fontWeight: 'bold',
        marginBottom: '0.5rem',
    },
});
