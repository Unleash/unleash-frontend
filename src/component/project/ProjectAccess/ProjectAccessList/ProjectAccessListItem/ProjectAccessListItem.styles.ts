import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
    iconButton: {
        marginLeft: '0.5rem',
    },
    actionList: {
        display: 'flex',
        alignItems: 'center',
    },
}));
